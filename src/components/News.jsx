import React from "react";
import { Select, Typography, Roe, Col, Card, Avatar } from "antd";
import moment from "moment";

import { useGetCryptoNewsQuery } from "../services/cryptoNewsApi.js";

const { Title, Text } = Typography;
const { Option } = Select;

const News = () => {
	return (
		<div>
			<h1>News</h1>
		</div>
	);
};

export default News;
